# Learning Real Analysis - Volume VI

Standalone split repository for **Volume VI: Computational Mathematics**.

This repository is a planned-volume stub created during the eight-volume migration. It contains the shared build support files, the `volume-vi/` source tree, and the canonical frontispiece asset `images/euler.png`.

Sync back to the monorepo is intentionally deferred. No active `sync-to-monorepo.yml` workflow is installed in this repository yet.
